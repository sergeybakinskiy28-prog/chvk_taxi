param($msg = "update")

# 1. Добавляем и коммитим
git add .
git commit -m $msg

# 2. Отправляем в облако (вместо save)
git push origin main

# 3. Перезапуск бота (если команда up делала именно это)
# Если up — это команда твоего хостинга, напиши её полностью.
# Если это просто запуск файла, то:
python main.py